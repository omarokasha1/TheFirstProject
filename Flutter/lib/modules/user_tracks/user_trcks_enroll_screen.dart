import 'package:flutter/material.dart';
class UserTracksEnrollScreen extends StatelessWidget {
  const UserTracksEnrollScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}