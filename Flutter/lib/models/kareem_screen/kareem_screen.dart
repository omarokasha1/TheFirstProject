
import 'package:flutter/material.dart';

class KareemScreen extends StatelessWidget {
  const KareemScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
